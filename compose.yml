version: '3'

services:
  tsp-bot:
    image: airopi/tsp-bot:latest
    build:
      context: .
      dockerfile: ./Dockerfile
      target: prod
    env_file:
      - .env
    tty: true
    restart: always
    volumes:
      - ./data:/app/data
      - ./resources:/app/resources
